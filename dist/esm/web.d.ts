import { WebPlugin } from '@capacitor/core';
import type { AddQueueItemsParams, AudioPlayerPlugin, GetItemProgressParams, GetQueueResult, ItemProgress, MoveQueueItemParams, PlaybackOptions, PlayerState, RemoveQueueItemParams, SeekParams, SetItemProgressParams, SetPlaybackOptionsParams, SetQueueParams, SetRateParams, SetRepeatModeParams, SetShuffleParams, SetVolumeParams, SkipToIndexParams, SyncQueueParams, SyncQueueResult } from './definitions';
export declare class AudioPlayerWeb extends WebPlugin implements AudioPlayerPlugin {
    private audio;
    private baseQueue;
    private shuffleQueue;
    private queue;
    private currentIndex;
    private stateRevision;
    private queueRevision;
    private status;
    private rate;
    private volume;
    private repeatMode;
    private shuffle;
    private playbackOptions;
    private progress;
    private suppressElementPlaybackEvents;
    private loadGeneration;
    private loadedItemId;
    private metadataPollTimer;
    private metadataFingerprintByItemId;
    private ensureAudio;
    private getMediaSession;
    private bumpStateRevision;
    private bumpQueueRevision;
    private getCurrentItem;
    private getCurrentPosition;
    private getCurrentDuration;
    private getInactiveStatusAfterLoad;
    private isCurrentItemLoaded;
    private withSuppressedElementPlaybackEvents;
    private resolveEffectiveQueue;
    private buildInitialShuffleQueue;
    private rebuildShuffleQueueAfterBaseChange;
    private updateEffectiveQueue;
    private resolveQueueIndex;
    private updateMediaSessionMetadata;
    private updateMediaSessionPlaybackState;
    private updateMediaSessionPositionState;
    private refreshMediaSessionState;
    private updateMediaSessionHandlers;
    private emitStateChange;
    private emitPassiveStateSnapshot;
    private emitTrackChange;
    private emitQueueChange;
    private resetAudioForEmptyQueue;
    private resetQueueState;
    private syncAudioSettings;
    private loadCurrent;
    private seekWithinLoadedCurrent;
    private startMetadataPollingIfNeeded;
    private stopMetadataPolling;
    private replaceItemEverywhere;
    private fetchAndApplyMetadata;
    private handleEnded;
    private buildState;
    setQueue(params: SetQueueParams): Promise<void>;
    syncQueue(params: SyncQueueParams): Promise<SyncQueueResult>;
    getQueue(): Promise<GetQueueResult>;
    addQueueItems(params: AddQueueItemsParams): Promise<void>;
    removeQueueItem(params: RemoveQueueItemParams): Promise<void>;
    moveQueueItem(params: MoveQueueItemParams): Promise<void>;
    clearQueue(): Promise<void>;
    play(): Promise<void>;
    pause(): Promise<void>;
    stop(): Promise<void>;
    seek(params: SeekParams): Promise<void>;
    private skipToPreviousInternal;
    skipToNext(): Promise<void>;
    skipToPrevious(): Promise<void>;
    skipToIndex(params: SkipToIndexParams): Promise<void>;
    setRate(params: SetRateParams): Promise<void>;
    setVolume(params: SetVolumeParams): Promise<void>;
    getState(): Promise<PlayerState>;
    setItemProgress(params: SetItemProgressParams): Promise<void>;
    getItemProgress(params: GetItemProgressParams): Promise<ItemProgress>;
    setPlaybackOptions(params: SetPlaybackOptionsParams): Promise<void>;
    getPlaybackOptions(): Promise<PlaybackOptions>;
    setRepeatMode(params: SetRepeatModeParams): Promise<void>;
    setShuffle(params: SetShuffleParams): Promise<void>;
}
